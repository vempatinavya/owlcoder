def permutation(l):
  if len(l)=0:
    return 0
  if len(l)=1:
    return 1
  lst=[]
  for i in range(len(l)):
    m=l[i]
    newlst=l[:i]+l[i+1:]
    for p in permutation(newlst):
      lst.append([m]+p)
  return lst
l=list('123')
for i in permutation(l):
  print(i)
